description = "Generate a marketing analysis report from the latest sales data"
prompt = """
1. Activate the `marketing-analyst` skill.
2. Find the latest `reports/files_generation_X/` folder.
3. Run the visualization engine:
   !{python generate_graphs_only.py}
4. Synthesize the findings into a professional Markdown report named `MARKETING_REPORT.md` inside `~/Desktop/eddy_marketing_insights_X/`, using the same `X` as the source folder.
5. Provide a link to the generated report in the final output.
"""
